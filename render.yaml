services:
  - type: web
    name: aura-agent
    env: python
    buildCommand: "cd my_agent && pip install -r requirements.txt"
    startCommand: "python my_agent/main.py"
    envVars:
      - key: PORT
        value: 10000
