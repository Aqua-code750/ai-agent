services:
  - type: web
    name: aura-agent
    env: python
    buildCommand: "pip install -r my_agent/requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: PORT
        value: 10000
