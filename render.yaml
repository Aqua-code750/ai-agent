services:
  - type: web
    name: aura-agent
    env: python
    buildCommand: "cd my_agent && pip install -r requirements.txt"
    startCommand: "cd my_agent && uvicorn server:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PORT
        value: 10000
